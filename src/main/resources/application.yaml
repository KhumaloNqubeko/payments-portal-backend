spring:
  application:
    name: payments-portal-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/payments_portal
    username: postgres
    password: 'Khumalo01'
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: false
        jdbc.time_zone: UTC

server:
  port: 8080
  ssl:
    enabled: true
    key-store: classpath:keystore.p12
    key-store-password: changeit
    key-store-type: PKCS12

  error:
    include-message: never
    include-binding-errors: never

app:
  frontend-url: https://localhost:5173
  jwt:
    secret: 8fK3!zQ9vLm2#xR7pT4wY6uB1cD8eG0hJkN5sAq
    expiration-minutes: 120
  security:
    require-https: true

logging:
  level:
    org:
      springframework:
        security: INFO
